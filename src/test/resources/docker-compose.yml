version: "3"

services:
  study-db:
    image: postgres
    ports:
      - 5432  # 테스트 시에는 포트 매핑을 하지 않아서 가용한 포트가 자동 할당되도록 한다.
    environment:
      POSTGRES_PASSWORD: study
      POSTGRES_USER: study
      POSTGRES_DB: study